Help me design an automated AI multi-project system. It's going to run on a cron. At each heartbeat it will check the user manifest. The user manifest should have a list of projects and their repo URLs, their priorities, and a list of maintenance tasks that should run on them. List of coating harnesses and list of providers and models. Kron will check the work tree folder which has all the repos following the naming convention repo name and if either its main or master or if it's related to an issue so should be repo dash issue number you know stash rebase to main or master and stash apply again and if there's a conflict it should mark on the project manifest that it's dirty It should then fetch all the open issues and the PR if the PR has no comments or reviews, it should dispatch the review agents which should also be part of the user manifest. Kappy, AI and so on. And they should follow a certain level of reviews from the ones that are free to the ones that are more expensive. So for example, it should first go through a loop of reptile and cappy AI reviews until they both agree it's perfect. Then have Gemini do another review, which is also free but has a usage limit. And then when that is satisfied, go to Codex review until that's satisfied. review until that's satisfied and finally go through a final deep seek for review and once all of those are agreed upon finally the user is notified that a pull request is ready to merge. It triggers an update on all documentation, agent files, does a cleanup on the branch. Each project has a a manifest stating what should be or not hierarchy of agents until the spec is perfect according to the issue or come up with follow-up questions design questions for the user those should be sent to the user through a set messaging platform such as telegram or slack should also be given a confidence level so that if a confidence is high enough it can trigger an auto development cycle which opens up the request creates the work tree, develops, goes through the review loop and again if the task is small enough it can be auto merged but also if the task is big has a large impact it should be breaking into smaller tasks controlled by our system and slowly solved and reviewed internally to create a PR. High impact PRs should never be automatically merged and should be checked with the user review. We should also have a Q&A system to run tests as much as possible for things that require an interface and we should also run checks for common issues with AI agents such as scope creep, deviation from the original issue, and also meaningless tests. And implementations should always start from the tests and they should always be meaningful we don't want maximum number of tests but we want very significant tests for our code that is very important and so for every heartbeat a log is created of what happened and the heartbeat checks the usage of all the providers and takes actions based on that. So it should be smart enough that if only low models are available, it will do low impact tasks. But when high models are available, it should do the high level impacts. We should set a limit for deep seek as it's currently the best model but it's only through API and it should have a limit per month per day whatever the user defines. So basically this is the automated agent system. User installs it on a remote machine and it runs on a heartbeat using as best as possible the usage limits saving limits when required working on days and times set by the user so that it doesn't conflict when the user is actually working. The user receives notifications every time tasks attention is needed for an action such as reviewing specs doing quality assurance or reviewing prior to merges help me create the spec for this system. And quick ticket. See what is missing, what makes sense and what doesn't.
